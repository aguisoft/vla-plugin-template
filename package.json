{
  "name": "my-vla-plugin",
  "version": "1.0.0",
  "description": "Mi plugin para VLA System",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "pack": "node scripts/pack.js",
    "release": "npm run build && npm run pack"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@vla/plugin-sdk": "file:./vendor/plugin-sdk",
    "typescript": "^5.3.3"
  }
}
